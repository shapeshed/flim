!!! xml
urlset(xmlns='http://www.sitemaps.org/schemas/sitemap/0.9')
  url
    loc http://shapeshed.com
    lastmod= new Date().toISOString() 
    changefreq= 'never'
    priority= '1.0'
  for post in posts
    url
      loc http://shapeshed.com/#{post.slug}
      lastmod= new Date().toISOString()
